[project]
name = "inventory-service"
version = "0.1.0"
description = "Deterministic inventory and reorder proposal API for the optional agents feature"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.139.2",
    "uvicorn==0.52.1",
    "pydantic==2.13.4",
]
